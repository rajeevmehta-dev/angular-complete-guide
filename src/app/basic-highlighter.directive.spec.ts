import { BasicHighlighterDirective } from './basic-highlighter.directive';

describe('BasicHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new BasicHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
